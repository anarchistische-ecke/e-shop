name: deploy-staging-runtime

permissions:
  contents: read
  packages: write

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to deploy to staging
        required: true
        default: main
        type: string

jobs:
  classify:
    runs-on: ubuntu-latest
    outputs:
      classification: ${{ steps.classify.outputs.classification }}
      runtime_safe: ${{ steps.classify.outputs.runtime_safe }}
      should_deploy: ${{ steps.classify.outputs.should_deploy }}
      destructive_files: ${{ steps.classify.outputs.destructive_files }}
      sha: ${{ steps.git.outputs.sha }}
      image_repository: ${{ steps.image.outputs.repository }}
      deploy_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || 'main' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.ref }}

      - name: Capture git metadata
        id: git
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Compute image coordinates
        id: image
        run: |
          owner_lc="${GITHUB_REPOSITORY_OWNER,,}"
          echo "repository=ghcr.io/${owner_lc}/eshop-api" >> "$GITHUB_OUTPUT"

      - name: Classify change set
        id: classify
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "push" ]] && [[ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            base_ref="${{ github.event.before }}"
          elif git rev-parse --verify HEAD^ >/dev/null 2>&1; then
            base_ref="HEAD^"
          else
            base_ref="$(git hash-object -t tree /dev/null)"
          fi
          bash ./scripts/classify-change-set.sh --base-ref "$base_ref" --head-ref HEAD >> "$GITHUB_OUTPUT"

  gate:
    needs: classify
    if: ${{ needs.classify.outputs.classification == 'destructive' }}
    runs-on: ubuntu-latest
    steps:
      - name: Block destructive staging runtime deploy
        run: |
          echo "Runtime-safe staging deploy is blocked because the change set is destructive."
          echo "Destructive files: ${{ needs.classify.outputs.destructive_files }}"
          echo "Use deploy-production-destructive.yml after staging verification instead."
          exit 1

  build:
    needs: classify
    if: ${{ needs.classify.outputs.runtime_safe == 'true' && needs.classify.outputs.should_deploy == 'true' }}
    runs-on: ubuntu-latest
    outputs:
      image_repository: ${{ needs.classify.outputs.image_repository }}
      image_tag: ${{ needs.classify.outputs.sha }}
      sha: ${{ needs.classify.outputs.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate committed Directus schema snapshot
        run: node scripts/directus-schema.js validate --snapshot directus/schema/schema.snapshot.json

      - name: Build Directus extensions
        run: bash ./scripts/directus-extensions-build.sh

      - name: Verify committed Directus runtime extensions are up to date
        run: git diff --exit-code -- directus/runtime-extensions

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build and test
        run: ./mvnw -B -ntp -pl api -am verify

      - name: Package API jar
        run: ./mvnw -B -ntp -pl api -am package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ needs.classify.outputs.image_repository }}:${{ needs.classify.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: [classify, build]
    if: ${{ needs.classify.outputs.runtime_safe == 'true' && needs.classify.outputs.should_deploy == 'true' }}
    runs-on: ubuntu-latest
    environment: staging
    concurrency:
      group: deploy-staging-runtime
      cancel-in-progress: false
    env:
      DEPLOY_REF: ${{ needs.classify.outputs.deploy_ref }}
      DEPLOY_SHA: ${{ needs.build.outputs.sha }}
      DEPLOY_RUN_ID: ${{ github.run_id }}
      YC_VM_HOST: ${{ secrets.YC_VM_HOST }}
      YC_VM_USER: ${{ secrets.YC_VM_USER }}
      YC_VM_SSH_KEY: ${{ secrets.YC_VM_SSH_KEY }}
      YC_VM_DEPLOY_PATH: ${{ secrets.YC_VM_DEPLOY_PATH }}
      GHCR_PULL_USERNAME: ${{ secrets.GHCR_PULL_USERNAME }}
      GHCR_PULL_TOKEN: ${{ secrets.GHCR_PULL_TOKEN }}
      API_IMAGE_REPOSITORY: ${{ needs.build.outputs.image_repository }}
      API_IMAGE_TAG: ${{ needs.build.outputs.sha }}
    steps:
      - name: Skip deploy when VM secrets are not configured
        if: ${{ env.YC_VM_HOST == '' || env.YC_VM_USER == '' || env.YC_VM_SSH_KEY == '' || env.YC_VM_DEPLOY_PATH == '' }}
        run: |
          echo "Skipping staging deploy because one or more VM deployment secrets are missing."

      - name: Deploy candidate runtime on staging VM
        id: deploy_remote
        if: ${{ env.YC_VM_HOST != '' && env.YC_VM_USER != '' && env.YC_VM_SSH_KEY != '' && env.YC_VM_DEPLOY_PATH != '' }}
        continue-on-error: true
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ env.YC_VM_HOST }}
          username: ${{ env.YC_VM_USER }}
          key: ${{ env.YC_VM_SSH_KEY }}
          command_timeout: 30m
          envs: DEPLOY_REF,DEPLOY_SHA,DEPLOY_RUN_ID,API_IMAGE_REPOSITORY,API_IMAGE_TAG,GHCR_PULL_USERNAME,GHCR_PULL_TOKEN
          script: |
            set -euo pipefail
            if [ -n "${GHCR_PULL_USERNAME:-}" ] && [ -n "${GHCR_PULL_TOKEN:-}" ]; then
              printf '%s' "$GHCR_PULL_TOKEN" | docker login ghcr.io -u "$GHCR_PULL_USERNAME" --password-stdin
            fi
            cd "${{ env.YC_VM_DEPLOY_PATH }}"
            git fetch origin "${DEPLOY_REF}"
            git checkout "${DEPLOY_REF}" || git checkout -B "${DEPLOY_REF}" "origin/${DEPLOY_REF}"
            git pull --ff-only origin "${DEPLOY_REF}"
            bash ./scripts/deploy-runtime-bluegreen.sh \
              --env-file .env \
              --compose-file docker-compose.prod.yml \
              --deploy-ref "${DEPLOY_REF}" \
              --deploy-sha "${DEPLOY_SHA}" \
              --api-image-repository "${API_IMAGE_REPOSITORY}" \
              --api-image-tag "${API_IMAGE_TAG}" \
              --run-id "${DEPLOY_RUN_ID}"

      - name: Stage remote diagnostics
        if: ${{ steps.deploy_remote.outcome == 'failure' }}
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ env.YC_VM_HOST }}
          username: ${{ env.YC_VM_USER }}
          key: ${{ env.YC_VM_SSH_KEY }}
          script: |
            set -euo pipefail
            cd "${{ env.YC_VM_DEPLOY_PATH }}"
            mkdir -p .deploy-state/export
            cp .deploy-state/latest-runtime-summary.txt .deploy-state/export/runtime-summary.txt 2>/dev/null || true
            latest_log="$(ls -1t .deploy-state/logs/*.log 2>/dev/null | head -n 1 || true)"
            if [ -n "$latest_log" ]; then
              cp "$latest_log" .deploy-state/export/
            fi

      - name: Download remote diagnostics
        if: ${{ steps.deploy_remote.outcome == 'failure' }}
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.YC_VM_HOST }}
          username: ${{ env.YC_VM_USER }}
          key: ${{ env.YC_VM_SSH_KEY }}
          source: "${{ env.YC_VM_DEPLOY_PATH }}/.deploy-state/export/*"
          target: "runtime-diagnostics-staging"

      - name: Upload diagnostics artifact
        if: ${{ steps.deploy_remote.outcome == 'failure' }}
        uses: actions/upload-artifact@v4
        with:
          name: staging-runtime-diagnostics-${{ needs.build.outputs.sha }}
          path: runtime-diagnostics-staging
          if-no-files-found: warn

      - name: Fail workflow after remote deploy error
        if: ${{ steps.deploy_remote.outcome == 'failure' }}
        run: |
          echo "Staging runtime deploy failed. See uploaded diagnostics."
          exit 1

  noop:
    needs: classify
    if: ${{ needs.classify.outputs.runtime_safe == 'true' && needs.classify.outputs.should_deploy != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip no-op runtime deploy
        run: echo "No runtime-affecting changes detected. Staging runtime deploy skipped."
